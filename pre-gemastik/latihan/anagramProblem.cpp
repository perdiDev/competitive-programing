// #include <bits/stdc++.h>
#include <string.h>
#include <iostream>
using namespace std;

int main() {
    string a = "nasi";
    // char a;
    cin >> a;

    // sort(a.start(), a.end);
    cout << a;

    return 0;
}
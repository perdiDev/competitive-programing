#include <bits/stdc++.h>
using namespace std;

#define cpyook ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define ll long long
#define endl "\n"

int main() {
    cpyook
    ll m, a, b, minX, maksX;
    cin >> m >> a >> b;

    minX = min(a, b);

    ll temp = m-minX;

    return 0;
}
#include <bits/stdc++.h>
using namespace std;

void cek(int n, int skor[]) {
    int m=3, k=3, s=3;

    for(int i=0; i<n-1; )
}

int main() {

    int jmlTim;

    cin >> jmlTim;
    int poinTim[jmlTim];

    for(int i=0; i<jmlTim; i++)
        cin >> poinTim[i];
    
    cek(jmlTim, poinTim);

    return 0;
}